<% if @jobs.empty? %>
  <p>
    You don't have any jobs defined.
    <%=
      link_to "Do you want to create a new job?",
        :controller => 'jobs',
        :action => 'new'
    %>
  </p>
<% else %>
  <%= render :partial => 'job', :collection => @jobs.ascending %>
<% end %>
